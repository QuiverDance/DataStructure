#include "LinkedGraph.h"
#include <iostream>
/*
int main(void)
{
	LinkedGraph* graph = new LinkedGraph(8, undirected);
	for (int i = 0; i < 8; i++)
		graph->addVertex(i);

	graph->addEdge(0, 1);
	graph->addEdge(0, 2);
	graph->addEdge(1, 3);
	graph->addEdge(1, 4);
	graph->addEdge(2, 5);
	graph->addEdge(2, 6);
	graph->addEdge(3, 7);
	graph->addEdge(4, 5);
	
	graph->displayGraph();

	std::cout << "DFS Traversal: " << std::endl;
	graph->traversalDFS(0);

	std::cout << "BFS Traversal: " << std::endl;
	graph->traversalBFS(0);
	delete graph;

}*/