#include "ArrayList.h"
#include <iostream>
/*
int main(void)
{
	ArrayList arrayList = ArrayList(10);
	ArrayListNode node;
	node.data = 0;
	for (int i = 0; i < 10; i++)
	{
		arrayList.addElement(i, node);
		node.data++;
	}

	arrayList.removeElement(4);

	node.data = 20;
	arrayList.addElement(4, node);
	arrayList.displayList();
	std::cout << arrayList.isFull() << std::endl;
	std::cout << "리스트 길이 : " << arrayList.getListLength() << std::endl;

	for (int i = 0; i < arrayList.getListLength(); i++)
	{
		std::cout << i << " : " << arrayList.getElement(i).data << std::endl;
	}

	arrayList.deleteList();
	arrayList.displayList();
}
*/